import { AppEvent } from '../types';

/**
 * Seeded initial application events for Phase 7 product analytics
 * Empty by default — real events are captured from user activity.
 */
export const INITIAL_APP_EVENTS: AppEvent[] = [];
